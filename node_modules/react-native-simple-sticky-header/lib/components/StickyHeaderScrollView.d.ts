import React from 'react';
import { View } from 'react-native';
export interface IStickyHeaderScrollView {
    children: React.ReactElement;
    top: (ref: React.LegacyRef<View>) => React.ReactElement;
    bottom: (ref: React.LegacyRef<View>) => React.ReactElement;
    headerBackground?: string;
    scrollViewBackground?: string;
    refreshing?: boolean;
    onRefresh?: () => void;
}
export declare function StickyHeaderScrollView({ children, top, bottom, headerBackground, scrollViewBackground, refreshing, onRefresh, }: IStickyHeaderScrollView): JSX.Element;
