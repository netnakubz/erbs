import { StyleProp, ViewStyle } from 'react-native';
export interface ICard {
    title: string;
    description: string;
    score: number;
    style?: StyleProp<ViewStyle>;
}
export declare function Card({ title, description, score, style }: ICard): JSX.Element;
